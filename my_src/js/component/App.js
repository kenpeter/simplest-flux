var React = require('react');
var AppAction = require('../action/AppAction'); // It needs to know what is on top his head.
var AppStore = require('../store/AppStore'); // It needs to know what is down there.


var App = React.createClass({


});


module.exports = App;
