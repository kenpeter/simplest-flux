var React = require('react');
var ReactDOM = require('react-dom');

// very fuzzy with the path, can get extension
var Component = require("./component/Component.js");

ReactDOM.render(
  <Component />,
  document.getElementById("main")
);
